# Band multi-agent SDK with LangGraph adapter
band-sdk[langgraph]==1.0.0

# LLM / framework
langchain-openai>=0.2
langgraph>=0.2
langchain-core>=0.3

# Webhook receiver
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# GitHub integration
PyGithub>=2.3

# Schemas / config
pydantic>=2.6
pyyaml>=6.0
python-dotenv>=1.0

# Static analysis / security tooling (invoked via subprocess by tools/)
ruff>=0.4
pip-audit>=2.7

# Dev / CI
black>=24.0
mypy>=1.10
pytest>=8.0
pytest-asyncio>=0.23
